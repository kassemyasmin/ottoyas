using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class PanelInventarioUI : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

   /* public void AgregarLineaInventario()
    {
        var linea = FindObjectOfType<InventarioUILine>();

        var lista = this.gameObject.GetComponentInChildren<ListaInventarioUI>();
        var nuevaLinea=Instantiate(linea.gameObject) ;
        nuevaLinea.transform.parent = lista.transform;

    }*/
}
